[project]
name = "nightd"
version = "0.1.0"
description = "A scheduler for autonomous coding agents"
readme = "README.md"
license = "FSL-1.1-ALv2"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.2",
    "ty>=0.0.17",
]
