[tools]
"github:supabase/cli" = "latest"
uv = "latest"

[tasks.format]
description = "Format code with ruff"
run = "ruff format ."

[tasks."linter:fix"]
description = "Fix linting issues with ruff"
run = "ruff check --fix ."

[tasks."type-check"]
description = "Run type checking with ty"
run = "ty check ."

[tasks.test]
description = "Run tests with pytest"
run = "pytest"

[tasks."dev:api"]
description = "Start API in dev mode"
run = "fastapi dev src/api/main.py"
dir = "packages/api"

[tasks.docs]
description = "Run documentation locally"
run = "zensical serve"

[settings]
python.uv_venv_auto = true

[env]
_.file = '.env'
