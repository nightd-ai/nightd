
[tasks.test]
description = "Run all checks on this project"
run = [
  "ruff format --check",
  "ruff check",
  "ty check",
  "pytest",
]

[settings]
python.uv_venv_auto = true

[env]
_.file = '.env'

[tools]
uv = "latest"
