services:
  mlflow:
    image: ghcr.io/mlflow/mlflow:v3.10.1-full
    container_name: nightd-mlflow
    command: mlflow server --host 0.0.0.0 --port 5000 --serve-artifacts
    ports:
      - "5000:5000"
    volumes:
      - mlflow-data:/mlflow
    working_dir: /mlflow

volumes:
  mlflow-data:
