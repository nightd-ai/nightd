version: "2"

linters:
  default: standard
  enable:
    - revive

linters-settings:
  revive:
    rules:
      - name: package-comments
        disabled: false

issues:
  exclude:
    - "unused-parameter"
    - "exported"
