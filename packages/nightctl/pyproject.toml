[project]
name = "nightctl"
version = "0.1.0"
description = "Command line interface for nightd"
requires-python = ">=3.14"

[tool.uv]
package = true

dependencies = [
    "httpx>=0.27.0",
    "typer>=0.24.1",
]

[project.scripts]
nightctl = "nightctl.cli:app"
